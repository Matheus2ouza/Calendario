const inputData = document.querySelectorAll(".date_event")

let inputdate = new Date(),
inputYear = date.getFullYear(),
inputMonth =date.getMonth(),
inputDay =date.getDay();

console.log(`${inputYear}$`)